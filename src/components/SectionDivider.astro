---
interface Props {
  color?: string;
}
const { color = '#6BD0DF' } = Astro.props;
---
<div
  class="w-full h-7 md:h-9"
  style={`background-color:${color}; clip-path: polygon(100% 60%, 100% 0%, 49% 0%, 0% 0%, 0% 60%, 49% 0%);`}
></div>